# v0.5.6 Verification Record

This record tracks the remaining verification work for StockPilot v0.5.6.

## Known CI findings addressed

- Browser E2E purchase-order action aligned with the actual `Create draft order` UI control.
- Reports E2E now uses a strict, deterministic currency assertion and includes the missing replenishment-readiness fixture.
- Release-readiness documentation now explicitly references the public `make release-check` target.

## Required final gate

The release is not considered stable until the final main commit has successful applicable:

- Release metadata consistency
- Release readiness
- Go quality
- Browser E2E
- PostgreSQL migration/readiness smoke test
- Web quality
- CodeQL
- Android checks, when applicable
- Extension checks, when applicable

No v0.5.6 tag should be created or moved before this gate is green.
