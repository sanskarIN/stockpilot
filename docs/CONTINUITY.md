# StockPilot development continuity

This file records the focused integration branch used to reconcile the replenishment/reporting work after platform CI was merged into `main`.

The integration approach intentionally preserves the existing granular commit history and avoids rewriting published branches.
