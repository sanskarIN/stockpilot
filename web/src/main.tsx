import { useEffect,useState } from "react";import{createRoot}from"react-dom/client";import{AuditScreen}from"./AuditScreen";import{Dashboard}from"./Dashboard";import{InventoryOperationsScreen}from"./InventoryOperationsScreen";import{LoginScreen}from"./LoginScreen";import{LotInventoryScreen}from"./LotInventoryScreen";import{ProductsWorkspaceScreen}from"./ProductsWorkspaceScreen";import{PurchaseOrdersWorkflowScreen}from"./PurchaseOrdersWorkflowScreen";import{ReportsScreen}from"./ReportsScreen";import{WarehouseLocationScreen}from"./WarehouseLocationScreen";import{stockpilotAPI}from"./api";import type{ReorderSuggestion,User}from"./types";import"./styles.css";import"./lotExpiry.css";import"./purchaseOrders.css";import"./warehouseLifecycle.css";import"./lotInventory.css";type SessionState={kind:"checking"}|{kind:"signed-out"}|{kind:"signed-in";user:User};type View="dashboard"|"products"|"inventory"|"orders"|"warehouses"|"lots"|"reports"|"audit";type InventoryOperation="stock_in"|"stock_out"|"adjustment"|"transfer";const inventoryOperations=new Set<InventoryOperation>(["stock_in","stock_out","adjustment","transfer"]);function App(){const[session,setSession]=useState<SessionState>({kind:"checking"}),[view,setView]=useState<View>("dashboard"),[reorderSeed,setReorderSeed]=useState<ReorderSuggestion|null>(null),[inventoryBarcode,setInventoryBarcode]=useState(""),[inventoryOperation,setInventoryOperation]=useState<InventoryOperation>("stock_in");useEffect(()=>{const params=new URLSearchParams(window.location.search);const barcode=params.get("barcode")?.trim()??"";const requested=params.get("inventoryOperation") as InventoryOperation|null;if(barcode&&requested&&inventoryOperations.has(requested)){setInventoryBarcode(barcode);setInventoryOperation(requested);setView("inventory");window.history.replaceState({},document.title,window.location.pathname)}else if(barcode){setInventoryBarcode(barcode);window.history.replaceState({},document.title,window.location.pathname)}let active=true;void stockpilotAPI.me().then(user=>{if(active)setSession({kind:"signed-in",user})},()=>{if(active)setSession({kind:"signed-out"})});return()=>{active=false}},[]);async function login(email:string,password:string){const result=await stockpilotAPI.login(email,password);setView("dashboard");setReorderSeed(null);setSession({kind:"signed-in",user:result.user})}async function logout(){try{await stockpilotAPI.logout()}finally{setView("dashboard");setReorderSeed(null);setSession({kind:"signed-out"})}}function expireSession(){setView("dashboard");setReorderSeed(null);setSession({kind:"signed-out"})}function openOrders(seed?:ReorderSuggestion){setReorderSeed(seed??null);setView("orders")}if(session.kind==="checking")return <main className="auth-page" aria-busy="true" aria-live="polite"><section className="auth-card session-check"><span className="brand-mark" aria-hidden="true">SP</span><p className="eyebrow">Secure workspace</p><h1>Checking your session</h1><p className="muted">StockPilot is verifying your signed-in session.</p></section></main>;if(session.kind==="signed-out")return <LoginScreen onLogin={login}/>;if(view==="products")return <ProductsWorkspaceScreen user={session.user} onBack={()=>setView("dashboard")} onSessionExpired={expireSession}/>;if(view==="inventory")return <InventoryOperationsScreen user={session.user} initialBarcode={inventoryBarcode} initialOperation={inventoryOperation} onBack={()=>{setInventoryBarcode("");setView("dashboard")}} onSessionExpired={expireSession}/>;if(view==="orders")return <PurchaseOrdersWorkflowScreen user={session.user} reorderSeed={reorderSeed} onBack={()=>{setReorderSeed(null);setView("dashboard")}} onSessionExpired={expireSession}/>;if(view==="warehouses")return <WarehouseLocationScreen user={session.user} onBack={()=>setView("dashboard")} onSessionExpired={expireSession}/>;if(view==="lots")return <LotInventoryScreen user={session.user} onBack={()=>setView("dashboard")} onSessionExpired={expireSession}/>;if(view==="reports")return <ReportsScreen user={session.user} onBack={()=>setView("dashboard")} onSessionExpired={expireSession}/>;if(view==="audit")return <AuditScreen user={session.user} onBack={()=>setView("dashboard")} onSessionExpired={expireSession}/>;return <Dashboard user={session.user} onLogout={logout} onSessionExpired={expireSession} onOpenProducts={()=>setView("products")} onOpenInventory={()=>setView("inventory")} onOpenOrders={openOrders} onOpenWarehouses={()=>setView("warehouses")} onOpenLots={()=>setView("lots")} onOpenReports={()=>setView("reports")} onOpenAudit={()=>setView("audit")}/>};const root=document.getElementById("root");if(!root)throw new Error("StockPilot root element was not found.");createRoot(root).render(<App/>);